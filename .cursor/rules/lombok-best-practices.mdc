---
description: 
globs: **/*.java
alwaysApply: false
---
---
name: lombok-best-practices.mdc
description: Best practices for using Lombok in Java
globs: **/*.java
---
- Use `@Data` for classes that require getters, setters, and toString methods.
- Prefer `@Value` for immutable classes to ensure thread safety.
- Use `@Builder` for complex object creation patterns.
- Avoid overusing Lombok annotations to maintain code readability.
